export type Hint = {
  playerId: string;
  avatarIndex: number;
  hint: string;
  isDelete: boolean;
};

export type Vote = {
  nickname: string;
  playerid: string;
  particIcon: number;
  text: string;
};
